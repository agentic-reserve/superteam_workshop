{
  "name": "Generate Client After IDL Change",
  "version": "1.0.0",
  "description": "Auto-generate TypeScript client when IDL is updated",
  "when": {
    "type": "fileEdited",
    "patterns": ["target/idl/*.json"]
  },
  "then": {
    "type": "runCommand",
    "command": "npm run codegen"
  }
}
